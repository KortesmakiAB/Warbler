def profile()
Why is it that "form = UserEditForm(obj=g.user)" (from forms.py) does not add the current password to the form?
Is this a security risk?

What is user "location" for if we never use it?
    Should I allow it to be an option in "edit" profile()?

No link to '/users'

problems with "unfollow" on '/users/
    does def stop_following() work?

What is generator folder? How does it work?

Look over HTML to see how it's organized.

Search for TODO's
